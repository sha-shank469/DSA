def compute_c_from_a_b(a,b):
    
    """
    Computes c such that a^2 + b^2 = c^2.
    Returns the value of c.
    """
    
    c = (a**2 + b**2)
    
    return c

a = 3
b = 4
print(compute_c_from_a_b(a, b))